name = "imdone-push"
main = "src/index.mjs"
compatibility_date = "2026-09-01"

# Secrets (set with `wrangler secret put NAME`):
#   FIREBASE_SERVICE_ACCOUNT  the JSON key of the Firebase service account
# Vars:
[vars]
FIREBASE_PROJECT_ID = "im-done-17215"
