gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# The DEBUG key is deliberately committed: it is not a secret and Google
# sign-in needs every build to carry the same signature. Release keys never.
!app/debug.keystore
